# FSRS 自動最適化（適応型ポリシー）設計 ―― 合格ラインを絶対に下げない前提で

対象: `denken3` / 電験3種 理論
作成時点の実データ: **2026-09-03**（Supabase 本番の実測に基づく。§1.2 は推測ではなく実値）

要求:
1. 手動で定数をいじるのをやめ、**日々の進捗と目標から毎日自動で最適化する**
2. 育児で全く勉強できない日が続くことを前提にする
3. **勉強できないからといって目標を下げない。合格ライン＋α は厳守する**

---

## 0. 最上位の原則（2026-09-03 訂正・以降のすべての設計はこれに従う）

> ### 不確実性は、必ず「もっとやる」側へ倒す。

要求3 の「設定を下げない」の**設定とは、`retention` や `daily_cap` のような明示的な
つまみだけを指さない。モデルの中の仮定も設定である。** 検証されていない楽観的な仮定を
使って「必要な学習量は少ない」と結論することは、つまみを下げるのとまったく同じ結果を招く
――むしろ、下げたことが本人にも見えないぶん悪質である。

「合格を確実にする」とは、**計画がモデルの間違いに耐えること**を意味する。

本書の初版（2026-09-03 午前）はこの原則を自ら破っていた。想定得点55点・必要ペース1.06問/日・
「1日約11分で合格ラインを守れる」と結論したが、その55点のうち **9.5点は1問も学習していない
5章から「5択の当て推量」で得られる前提の点数**であり、残りも未検証の `STATUS_PROB.A = 0.92`
（解答を見た後の自己申告）に乗っていた。§2.1 でその誤りを訂正する。

### 派生する具体則

1. **未着手は 0点**。当て推量による得点を計画に組み込まない
2. 検証されていない正答確率は、もっともらしい範囲の**悲観側**を採る
3. 忘却を得点に反映する（現行は期限切れの A も満額で数えている・未対応）
4. **「合格に必要な最小集合」モードは、推定が実測で検証されるまで無効**。既定ゴールは全範囲完走

---

## 1. 現状の棚卸し ―― 「どうなっているか分からない」の正体

### 1.1 設定は DB にほぼ存在せず、すべてコード内の定数である

これが「幾度となく最適化を試みたが現状が分からない」の原因。**設定値はコミット履歴の中にしかなく、
アプリのどこからも見えない。** 実際に永続化されているのは `denken_exam_plans` の日付だけ。

| | 実体 | 状態 |
|---|---|---|
| `denken_settings.daily_cap` | migration 002 で作成 | **デッドカラム**。コード上どこからも読まれていない（`pace.ts` のコメントに名前が残るのみ）。行そのものも未作成 |
| 時間予算（5/15/30分） | `App.tsx` の `useState<number \| null>(null)` | **永続化されていない**。リロードで消える |
| FSRS パラメータ一式 | `src/lib/*.ts` のモジュール定数 | 変更＝コミット。アプリから可視でも可変でもない |

### 1.2 全パラメータの一覧（現在の値）

**A. FSRS 本体（`fsrs.ts`）**

| 定数 | 現在値 | 意味 |
|---|---|---|
| `RETENTION_DEFAULT` | **0.85** | 目標保持率（既定） |
| `RETENTION_ENDGAME` | 0.90 | 直前期の保持率 |
| `RETENTION_ENDGAME_DAYS` | 60 | 直前期の定義（試験60日前から） |
| `FINAL_CHECK_DAYS_BEFORE_EXAM` | 21 | S の試験前最終確認 |
| `RATING_MAP` | A→Easy / B→Good / C→Again | **`Hard` 未使用** |
| `clipDueToExam` テーパー | 残28日→上限14日 / 残14日→上限7日 | 直前期の間隔圧縮 |
| ts-fsrs `w[]` | **デフォルトのまま** | 一度も最適化していない |
| 初期難易度 D | 5（`createEmptyCard` 既定） | 計算/暗記で共通 |

**B. 復習の選択（`reviewPlan.ts`）**

| 定数 | 現在値 |
|---|---|
| `STATUS_WEIGHT` | C:1.0 / B:0.7 / A:0.45 / S:0.25 |
| `RISK_FLOOR` | 0.15 |
| `BAND_HIGH_MARGIN` | 0.10（🔴は R < 保持率−0.10） |
| `catchUpDays()` | 残14日→1日 / 残30日→2日 / それ以外→**3日** |

**C. ペース（`pace.ts`）**

| 定数 | 現在値 |
|---|---|
| `EWMA_HALF_LIFE_DAYS` | 14 |
| `REPLAN_LATE_DAYS` | 14 |
| `DEFAULT_BUNYA_LEAD_DAYS` | 90 |
| `MIN_NENDO_DAYS` | 30 |
| `DEFAULT_ATTEMPTS_PER_MASTERY` | 2（上限5） |
| **推奨ノルマ** | **`clamp(必要ペース, 現在ペース×0.8, 現在ペース×1.3)`** ← §3.1 で問題にする |

**D. 目標（`passTarget.ts` / `analytics.ts`）**

| 定数 | 現在値 |
|---|---|
| `DEFAULT_PASS_MARGIN` | **10 点**（目標＝合格点+10＝70点） |
| `STATUS_PROB` | S/A:0.92 / B:0.60 / C:0.25 / 未着手:0.20 |
| `BASELINE` | 0.20（5択の当て推量） |
| `passingScore` | 60 |
| ゴール切替 | `pass` 達成 → `mastery`（全問A以上）へ自動昇格 |

**E. 時間（`estimateMinutes.ts` / `timer.ts`）**

`DEFAULT_SECONDS`（難易度×モード表）／`STATUS_FACTOR` 未着手・C:1.0 / B:0.7 / A・S:0.5 ／
`MIN_SAMPLES` 5 ／ `MAX_DURATION_SECONDS` 15分・cap = 中央値×3

### 1.3 実データ（2026-09-03 時点・本番DBの実測）

| 項目 | 実値 |
|---|---|
| 収録問題数（理論） | **440問**（9章。`totalCount` は全章 `questions.length` と一致＝未収録分ゼロ） |
| 進捗行数 | 232問（うち **A:179 / B:36 / S:17 / C:0**）。未着手 208問 |
| 総演習回数 | **526回** |
| 学習期間 | 2026-07-17 〜 **2026-08-20** |
| **最終学習日からの経過** | **14日（8/21〜9/3 が完全停止中）** |
| 期限到来（due） | **106問**（A:85 / B:21）。最古の due は 8/14 |
| 平均 stability | 32.2日（due 分のみ 11.5日） |
| 平均 difficulty | **2.53**（FSRS の 1〜10 スケール。かなり「簡単」寄りに寄っている） |
| 総 lapse | 22 |
| 1問の実測解答時間 | 日次平均 240〜717秒（**おおむね4〜12分**） |
| **CBT模試の実測** | **0件**（`denken_mock_sessions` が空） |
| 試験日 | **2027-02-06**（今日から156日） |
| 分野別完走 目標日 | 2026-11-29（**87日後**） |
| 申込締切 | 2026-11-26 |

---

## 2. 実データから判明した事実 ―― 危機感の方向が実態とずれている

### 2.1 【訂正】合格には遠く、必要なのは1日約50分である

**初版の誤り。** 当初この節は「合格ラインに対し必要ペースの3〜4倍で先行しており、
1日約11分で足りる」と結論していた。誤りであり、撤回する。

実データでは学習済みは **4章232問のみ**（直流69・単相交流69・三相交流22・静電気72）。
**残り5章208問（過渡現象14・電磁気55・電気計測45・電子理論42・電子回路52）は1問も
手つかず**である。この状態で想定得点が55点になるのは、モデルの側に原因がある。

```
旧モデル: [ 196×0.92 + 36×0.60 + 208×0.20 ] ÷ 440 × 100 = 55.3点
             学習済み     B判定      未着手5章
             45.9点                   9.5点  ← 「5択だから当て推量で当たる」
```

**55点のうち 9.5点は、1問も見ていない5章からまぐれ当たりでもらえる前提の点。**
合格を確実にする計画に、まぐれ当たりを組み込んではならない。加えて `A = 0.92` も
未検証で、実データに C が1件も無い（A:179 / B:36 / S:17 / C:0）ことから、
自己評価が甘い側に寄っている疑いがある。

### 是正後の数字

`STATUS_PROB` を保守側へ倒した（未着手 0.2→**0** / C 0.25→**0.15** /
B 0.6→**0.35** / A・S 0.92→**0.75**、`BASELINE` 0.2→**0**）。

| 前提 | 想定得点 | 70点まで | 必要問数 |
|---|---|---|---|
| 旧（未着手0.2・A=0.92） | 55点 | 15点 | 92問 |
| 未着手を0点にするだけ | 46点 | 24点 | 115問 |
| **是正後（A=0.75）** | **36点** | **34点** | **198問** |

残り5章は208問。**つまり実質「残り全部やる」が必要量**であり、「最小集合92問」は
未着手章からタダで9.5点もらえる前提でしか成立しない数字だった。

```
新規208問 × 平均2.5回 × 5分   ≒ 43時間
既存232問の維持               ≒ 29時間
                      合計    ≒ 72時間 ÷ 87日 = 1日 約50分
```

**1日11分ではなく、約50分。** しかも育児で休む日がある以上、動ける日はこれ以上を要する。

### 2.2 【最大の問題】復帰した日に「今日36問・約4時間」の崖が立つ

`planDailyReviews()` の推奨ラインは

```
recommendedCount = min(due総数, max(urgentCount, ceil(due総数 ÷ catchUpDays), 1))
catchUpDays = 3（試験まで87日あるため）
→ ceil(106 ÷ 3) = 36問  → 実測時間で 約4時間
```

**この式には「その人が今日どれだけ時間を使えるか」が一切入っていない。** 締切からの逆算だけで
決まる。14日休んで復帰した日に「今日36問・約4時間」と出れば、育児中には確実に折れる。

しかも §2.1 のとおり、合格に必要なのは 1日1問・11分である。**アプリは、合格に不要な負荷で
利用者を潰す構造になっている。** これが自動最適化で最初に直すべき点。

### 2.3 【検証の空白】想定得点55点は、一度も検証されていない

- `denken_mock_sessions` が **0件** ＝ CBT 実測がなく、`ScoreEstimate.actual` / `gap` が常に null
- 理解度 **C が1件も無い**（A:179 / B:36 / S:17 / C:0）。「答えを見た」を一度も記録していない
- 平均 difficulty が **2.53** と低い ＝ `RATING_MAP` で A→`Easy` が179回流れ、FSRS が
  「この人にとって簡単」と学習し続けている

`STATUS_PROB.A = 0.92` は「Aを付けた問題は本番で92%正解できる」という仮定だが、
**その A 自体が「解答を見た後の自己申告」である**（Phase 1 の設計書 §1.2①）。
C が0件という分布は、自己評価が甘い側に寄っている可能性を示す。55点は上振れしている恐れがあり、
**この不確実性こそが「合格を確実にする」ことへの最大のリスク**である。

### 2.4 【バグ】S の17問が、試験前最終確認から永久に漏れている

`fsrs.ts` の `finalCheckDue()`（§6-4・課題4）は、S にした問題を試験21日前＝**2027-01-16** に
一度だけ復習キューへ戻す仕様。しかし実データでは **S:17問すべてが `due_date IS NULL`**。

原因: この17問は 2026-07-26（15問）と 2026-08-11（2問）に記録されており、
`finalCheckDue` の実装（Phase D・2026-09-01）**より前**。当時の S は due_date=null を書いていた。
`deriveFromHistory` は次に記録したときに再導出して直すが、**S は due_date=null のため
復習キューに出てこない → 記録される機会が来ない → 永久に直らない**（デッドロック）。

影響: 最も確実な得点源17問が、試験直前に一度も確認されない。**要求3（合格を確実に）に直接反する。**

修正は一度きりのバックフィル1行で済む（§5 Phase 0）。

---

## 3. 自動最適化の設計

### 3.1 現行の推奨ノルマが持つ構造的欠陥

```ts
recommendedNorm = max(1, ceil(clamp(requiredPace, currentPace×0.8, currentPace×1.3)))
```

**現在ペースが下がるとノルマも下がる。** 育児で停止が続けば `currentPace → 0` となり、
上限 `currentPace×1.3 → 0` に張り付いて推奨は `max(1, …) = 1問`。
**「勉強できないから要求を下げる」という、利用者が明確に禁止した挙動そのもの。**

同時に `verdict = 'behind'` が出続け、毎日「遅れている」と言われる（課題2 で既出）。
つまり現行は「要求は下げるのに、判定は責める」という最悪の組み合わせになっている。

### 3.2 正しい分離 ―― 「目標」は下げない、「今日の範囲」は絞る

物理的に、供給（時間）< 必要（学習量）のとき、動かせるものは4つしかない:

| 選択肢 | 本設計での扱い |
|---|---|
| ① 目標得点を下げる | **禁止**（要求3） |
| ② 期日を延ばす | 試験日は動かせない。ただし**科目合格制があり「今回は理論だけ確実に」は正当な戦略**。判断材料は出すが自動では動かさない |
| ③ 範囲を絞る | **自動化する**（コア集合／バッファ集合の分離） |
| ④ 効率を上げる | **自動化する**（保持率の問題別最適化） |

したがって自動最適化の本体は ③ と ④ である。**ノルマではなく、キューの中身を差し替える。**

### 3.3 三層のポリシー

毎日1回、純関数 `optimizePolicy()` が以下を算出する。

```ts
// src/lib/policy.ts（新規）
export interface Policy {
  passMargin: number            // 層1: 動的な安全マージン（点）
  coreIds: Set<string>          // 層2: 合格に必要な最小集合（コア）
  retentionOf(id: string): number // 層3: 問題ごとの目標保持率
  dailyFloor: number            // 停止中でも切らない最低ライン（問）
  feasibility: Feasibility      // 供給 vs 必要 の実現可能性
}
```

#### 層1: 安全マージンを、推定誤差から自動で決める

現行は `DEFAULT_PASS_MARGIN = 10` 固定。「合格を確実に」するなら、**推定が信用できないほど
マージンを厚くする**のが正しい。

```ts
// CBT実測が無い間は最大マージン。実測が増えるほど、実測とのズレの大きさで決める。
function passMargin(est: ScoreEstimate, sessions: MockSession[]): number {
  const finished = sessions.filter(s => s.status === 'finished' && s.score != null)
  if (finished.length === 0) return 15          // 未検証 → 厚く（現行10より保守的）
  const bias = mean(finished.map(s => est.estimate - s.score!))  // 正 = 想定が甘い
  return clamp(8 + Math.max(0, bias), 8, 20)    // 甘い分だけ上乗せ
}
```

現状（実測0件）では **マージン15点＝目標75点**。requiredQ は 92問 → 約123問へ増える
（必要ペース 1.4問/日）。**目標を上げる方向に自動で動く**のが要求3に沿う。
CBT を受けて想定が正しいと分かれば自動で 8点まで下がり、無駄な負荷が消える。

#### 層2: コア集合とバッファ集合を分ける

`passTarget.planPassTarget()` は既に「目標到達に必要な最小の問題集合」の**問数**を返している。
これを**問題IDの集合**まで返すよう拡張する（伸びの降順に積む処理は既にある。返り値に
`coreIds` を足すだけ）。

| 集合 | 定義 | 扱い |
|---|---|---|
| **コア** | 残り5章208問（全範囲完走）＋ 既に A の問題の維持 | 保持率を高く保つ。時間が無い日はここを点数影響順に出す。**総量は減らさない** |
| **バッファ** | 上を満たしたうえでの上積み | 保持率を下げてよい。後回しにできる |

**【訂正・2026-09-03】** 初版はコアを「合格に必要な最小集合」と定義していたが、これは
原則 §0 に反する（＝やる量を減らす仕組み）。加えて実データでは最小集合92問がすべて
**未着手問題**になり、期限到来している106問（A:85 / B:21）が1問もコアに入らない。
既にAの179問を放置すれば忘れて落ち、想定得点が下がる。**A が C へ落ちる損失（0.15点/問）は
新規着手の伸び（0.17点/問）とほぼ同じ大きさ**であり、維持と前進は同じ土俵で並べる必要がある。

したがってコアは2種類を含む:
- **前進コア** … 未着手を A にして点を積む（残り5章208問）
- **維持コア** … 既に A の問題を落とさない（179問）

今日の並び順は「**1問あたりの点数影響 ÷ 所要時間**」で決める。時間が無い日でも、その時間は
合格に最も効く使われ方をする ―― ただし**やらなかった分は消えず、翌日以降に必ず戻る**。

#### 層3: 保持率を問題ごとに決める

現行 `retentionFor(eventDate, examDate)` は日付だけを見る単一のグローバル値。これを拡張する。

```ts
function retentionOf(id: string, mode: EstimateModeKey, policy: Policy, date, examDate): number {
  const base = policy.coreIds.has(id)
    ? 0.90                                    // コア: 落とさない
    : (mode === 'calc' ? 0.85 : 0.80)         // バッファ: 回転優先
  return diffDays(date, examDate) <= 60 ? Math.max(base, 0.90) : base
}
```

計算/暗記の分離（Phase 1 設計書 §4.2）は、この式に吸収される。
**総復習量が増えないよう、コアを上げた分はバッファを下げて相殺する**（§6 で実測検証）。

### 3.4 【最重要】決定的再生を壊さないための仕組み

`fsrs.ts` には明示的な制約がある:

> retention を「その実施日から試験日までの残日数」だけで決めるのが要点で、こうすると
> `deriveFromHistory` が `review_history` を再生するたびに同じ結果になる（決定的）。

**ポリシーが日々変わると、この決定性が壊れる。** 過去の予定日が毎日書き換わり、
何が起きているか誰にも分からなくなる ―― まさに利用者が困っている「現状が分からない」状態の悪化。

解決: **その日に適用したポリシーを、履歴エントリに書き残す。**

```ts
export interface ReviewHistoryEntry {
  date: string
  status: Status
  prev?: ReviewSnapshot
  duration_seconds?: number
  attempt?: Attempt                  // Phase 1（解答前コミット）
  policy?: { retention: number }     // ← 追加。記録時に実際に使った目標保持率
}
```

再生時は `entry.policy?.retention ?? retentionFor(entry.date, examDate)` とする。
**旧データは従来式にフォールバックするので後方互換が完全に保たれ、新データは記録時の値で
再生されるので決定的性も保たれる。** Phase 1 の `attempt` と同じ JSONB の器に入るため、
**マイグレーションは不要。**

### 3.5 停止期間の扱い ―― 下げるのではなく、絞る

```ts
// 今日の推奨ラインを、締切だけでなく「使える時間」と「コア集合」から決める。
export function planToday(candidates, policy, budgetMinutes, stats): TodayPlan {
  const core = candidates.filter(c => policy.coreIds.has(c.question.id))
  const buffer = candidates.filter(c => !policy.coreIds.has(c.question.id))
  // ① コアの高リスク分は、予算を超えても必ず線の上に置く（切らない）
  // ② 残った予算をコアの残り → バッファ の順に埋める
  // ③ 予算が未設定・ゼロでも dailyFloor（1〜3問）は必ず出す
}
```

| 状況 | 現行の挙動 | 改訂後 |
|---|---|---|
| 14日停止から復帰 | 「今日36問・約4時間」 | 「今日 **コア3問・約18分**」＋「バッファ103問は順番待ち」 |
| ペースが落ちた | **ノルマが自動的に下がる**（禁止事項） | ノルマは下げない。**バッファが後ろへ流れるだけ** |
| 判定 | 毎日 `behind` | コア基準で判定。現状は `ahead` |
| 5分しかない日 | 予算で上から36問を切る | **コアの最高リスク1問**を出す |

### 3.6 実現可能性は「下げる」のではなく「事実として突きつける」

コアだけでも供給が足りなくなったときは、**黙って目標を下げてはならない**。
`Feasibility` を計算して事実を提示する:

```ts
export interface Feasibility {
  requiredMinutesPerDay: number   // コア完遂に必要な 分/日
  availableMinutesPerDay: number  // 直近の実績から推定した 分/日（EWMA）
  verdict: 'safe' | 'tight' | 'shortfall'
  // shortfall のとき提示する選択肢（自動では選ばない）
  options: ('increase_time' | 'defer_exam' | 'accept_risk')[]
}
```

現状は `requiredMinutesPerDay ≒ 11分` に対し、実績（停止前）は数十分〜数時間。**`safe`**。
`shortfall` になって初めて「次回試験へ回す」等を**利用者に選ばせる**。
アプリが勝手に目標を下げることはしない。

---

## 4. ts-fsrs の重み `w[]` 最適化を、今はやらない理由

「FSRS の最適化」といえば通常は `w[]`（17パラメータ）のフィッティングを指すが、
**現時点では実施すべきでない。** 根拠は3つ、いずれも実データによる。

1. **母数が足りない。** 総演習 **526回**。FSRS プロジェクトの一般的な目安は
   最適化に 1,000件以上、安定させるには数千件。526件では過学習になる
2. **入力の情報量が無い。** Rating は実質 `Easy`(A:179) と `Good`(B:36) の2値。
   **C が0件で `Again` がほぼ流れておらず、`Hard` は `RATING_MAP` に存在しない。**
   忘却曲線の当てはめは「失敗した記録」から情報を得るので、失敗がほぼ無いデータで
   フィッティングしても意味のある `w` は出ない
3. **入力の妥当性が未検証。** Rating の元である A/B は解答を見た後の自己申告で、
   客観的な正誤データが1件も無い（Phase 1 設計書 §1.2①）。**間違った入力で最適化すれば、
   間違いが固定される。**

`ts-fsrs@5.4.1` はスケジューラであり、`w[]` の最適化器は同梱していない
（別パッケージ `fsrs-rs-nodejs` 等が必要）。**インストール済みの版で最適化APIの有無を
実装前に確認すること。**

**正しい順序**: Phase 1 で客観的な正誤を貯める → `Rating.Hard` と `Again` を実際に流す
→ 演習1,000件を超える → そこで初めて `w[]` 最適化を検討する。
それまでは §3 の「入力パラメータの自動最適化」で十分に効果が出る。

---

## 5. ロードマップ（Phase 1 と並行して進められる）

### Phase 0 —— 今すぐ直すべき1点（単独で即マージ可）

S の17問を最終確認へ戻すバックフィル。`012` / `013` と同じ「データ是正マイグレーション」の形。

```sql
-- 015_s_final_check_backfill.sql（番号はコミット直前に再確認）
-- finalCheckDue 実装前に S を付けた問題は due_date=null のまま残り、
-- 復習キューに出ないため deriveFromHistory による自己修復が永久に起きない。
-- 試験21日前（2027-02-06 − 21日）へ一度だけ戻す。
UPDATE denken_reviews r
SET due_date = p.exam_date - 21
FROM denken_exam_plans p
WHERE r.user_id = p.user_id AND r.exam_id = p.exam_id
  AND r.status = 'S' AND r.due_date IS NULL
  AND p.exam_date - 21 > CURRENT_DATE;
```

併せてアプリ側にも再発防止を入れる（試験日を設定・変更したとき、S の due を計算し直す）。

### Phase A —— ポリシーの可視化（最適化はまだしない）

**「現状が分からない」を先に解消する。** 自動で動かす前に、いま何が効いているかを画面に出す。

| # | 内容 | 変更 |
|---|---|---|
| A-1 | `Policy` 型と `optimizePolicy()`（値を返すだけ。まだ FSRS へ流さない） | **新規** `src/lib/policy.ts` |
| A-2 | `planPassTarget()` が `coreIds` も返すよう拡張 | `passTarget.ts`（返り値に1フィールド追加） |
| A-3 | 設定タブに「いま効いているパラメータ」カード（保持率・マージン・残り問数・必要ペース・必要分/日） | `SettingsView.tsx`（末尾にカード1枚） |
| A-4 | **表示する値は保守側のみ**。楽観値は並記しない。想定得点には「CBT実測で未検証」を明示する | 同上 |
| — | マイグレーション | 不要 |

### Phase B —— 今日のラインを容量ベースへ（体感が最も変わる）【実装済み】

| # | 内容 | 変更 |
|---|---|---|
| B-1 | `planToday()`: 点数影響÷所要時間の順＋予算＋`dailyFloor`。**総量は減らさず順序を変えるだけ**。積み残しは「順番待ち」として翌日以降へ必ず戻す | **新規** `src/lib/planToday.ts`（`planDailyReviews` は残す） |
| B-2 | 時間予算を DB へ永続化（`denken_settings` の死んだ `daily_cap` を使わず新カラムを追加） | `App.tsx`、migration `016_settings_time_budget.sql` |
| B-3 | `TodayPanel` に「前進 n問 / 維持 n問 / 順番待ち m問」の内訳 | `TodayPanel.tsx` |
| B-4 | `Feasibility` バナー（`shortfall` のときだけ表示）<br>※ Phase B 後に、ペースカードの「遅延が続いています」ブロックと**1枚へ統合**（`shortfall` または遅延の持続で表示・下の 5. を参照） | `DashboardView.tsx`<br>→ `lib/planAlert.ts` ＋ `features/dashboard/PlanAlert.tsx` |

#### 実装時の判断（設計との差分）

1. **点数影響の定義。** 前進は「A まで引き上げたときの伸び ÷ そこに要する演習回数」、
   維持は「忘却リスク(1-R) × A→C の損失」とした。前進を回数で割らないと密度が維持の
   5倍以上になり、毎日キューの上を未着手が占めて既存の A が復習されなくなる。
   割って初めて §3.3 が言う「同じ土俵（0.17点／0.15点）」に並ぶ。
2. **最低ラインの適用範囲。** `dailyFloor`（1〜3問）は**予算が未設定・ゼロのとき**の下限。
   利用者が 5分 を明示的に選んだ日は 1問。5分と申告した人に3問（≒23分）を出すのは、
   小さくなっただけの同じ崖であり、Phase B が消そうとしているものだから（§3.5 の表の
   「5分しかない日 → コアの最高リスク1問」に合わせた）。
3. **新規着手の区切り（課題3の小見出し）を撤去。** 復習due と新規着手枠が1本の順序に
   統合され、キューの途中に境界が存在しなくなったため。内訳は今日パネルの1行が担う。
4. **`pace.recommendedNorm` の上限 `現在ペース×1.3` を撤去（§3.1）。** 上限があると
   停止中に推奨ノルマ自体が下がり、同時に `behind` を出し続ける。下限（×0.8）は残した
   ―― 先行している人を必要ペースまで落とすのは、同じ「下げる」を逆向きにやることになる。
   今日の前進枠も `recommendedNorm` ではなくポリシー（残り ÷ 残り日数）から決める。

5. **警告を1枚へ統合し、選択肢を3つに揃えた（B-4 の後日改訂）。** 実装当初は
   実現可能性バナー（赤・`shortfall`）とペースカードの軌道修正ブロック（黄・遅延の持続）が
   同時に立ち、選択肢が合計6項目に膨らんでいた。原因はどちらも1つ（供給 < 必要）で
   選択肢も重複しており、**常時2枚出る警告は読み飛ばされる**。文面の組み立てを
   `lib/planAlert.ts`（純関数）に寄せ、表示条件は旧2枚の OR とした。
   選択肢は §3.6 の `Feasibility.options` と同じ3キーに揃えている。
   - **科目合格制は `increase_time` の中に置いた。** §3.2 の表は ②期日を延ばす に
     置いているが、実現可能性は科目ごとの `denken_exam_plans` とその科目の収録問題だけで
     必要量を積むため、他科目を次回へ回しても**必要 分/日 は減らない**。効くのは供給側
     （他科目の時間がこの科目へ回る）なので、①の一手段として提示するのが実態に合う。
     「判断材料は出すが自動では動かさない」は変えていない。
   - **「範囲を絞る（目標を合格ライン到達へ切り替える）」は選択肢から外した。**
     `passTarget.isEstimateValidated`（CBT実測2回）が通るまで無効なモードで、
     利用者がいま押せる手ではないため。何が必要かは注記1行で示す。

#### 実測（実データ相当・106問due＋前進枠3問）

| | 改訂前 | 改訂後 |
|---|---|---|
| 14日停止から復帰（予算なし） | 36問・約4時間 | **6問・約46分**（順番待ち103問） |
| 5分しかない日 | 予算で上から36問を切る | **1問**（順番待ち108問） |
| 忘却が進行したコア | 予算で切られうる | **必ず線の上**（超過は画面に明示） |
| 総量 | ― | 今日＋順番待ち＝候補総数（**減らない**） |

### Phase C —— 保持率の自動最適化（決定的再生を保護しつつ）

| # | 内容 | 変更 |
|---|---|---|
| C-1 | `ReviewHistoryEntry.policy` を記録（**先に記録だけ始める**） | `types.ts`（1行）、`App.tsx` |
| C-2 | `retentionOf()` をコア/バッファ×studyMode で分岐 | `fsrs.ts`、`reviewPlan.ts` |
| C-3 | `deriveFromHistory` が `entry.policy.retention` を優先 | `fsrs.ts` |
| C-4 | 動的マージン（CBT実測とのズレから） | `policy.ts` |
| — | マイグレーション | **不要**（JSONB） |

**マージ条件**: `policy` を持たない旧履歴で `deriveFromHistory` の結果が改修前と完全一致すること。

### Phase D —— `w[]` 最適化の判断（演習1,000件超・Rating 4値が流れてから）

現時点では着手しない（§4）。

---

## 6. 批判的検証

| 論点 | 検証 | 結論・打ち手 |
|---|---|---|
| **そもそも危機なのか** | 実データでは合格ラインに対し必要ペースの3〜4倍で先行。必要なのは1日約11分 | **現時点で危機ではない。** 目標を下げる必要も無い。要求3は無償で満たせる。ただし §2.3 の未検証リスクが残るため、マージンを厚くして吸収する |
| 自動最適化は「勝手に動く」ぶん、現状がさらに見えなくなるのでは | **正当な懸念。** 利用者の一次不満は「現状が分からない」ことであり、自動化はそれを悪化させ得る | だから **Phase A（可視化）を Phase C（自動化）より先に置いた。** 画面に出ていない値は自動で動かさない |
| ポリシーを日々変えると `deriveFromHistory` の決定性が壊れる | 現行コードが明示的に守っている最重要の性質 | `entry.policy` に記録して再生時に使う（§3.4）。旧データは従来式へフォールバック。**この仕組み無しに層3を実装してはならない** |
| コア集合が毎日入れ替わって落ち着かないのでは | requiredQ は想定得点に依存し、A が増えるたびに縮む。境界の問題が出たり入ったりし得る | コア集合に**ヒステリシス**を入れる（一度コアに入った問題は、目標を10%超過して余裕が出るまで外さない）。実装は `policy.ts` 内に閉じる |
| マージンを15点に上げると、要求される問数が92→123問へ増える | 負荷は増える | それでも必要ペースは 1.4問/日・1日約15分で、実績の範囲内。**「確実に合格」を優先する要求3に沿った増やし方**であり、下げる方向の妥協ではない |
| バッファを実質切り捨てるのは、満点狙いを放棄することでは | そのとおり | 現行も `pass` 達成後に `mastery` へ自動昇格する設計になっている。**コアを固めたら自動でバッファがコアに繰り上がる**ので、時間があれば結果的に全問へ向かう |
| `STATUS_PROB.A = 0.92` の妥当性が未検証のまま全計算の土台になっている | **最大の弱点。** 想定得点55点も requiredQ 92問も、この1つの仮定に乗っている | 実測で較正するしかない。**CBT模試を1回受けることが、どの自動最適化よりも価値が高い**（§7） |
| C が0件なのは自己評価が甘いからでは | 断定はできない（実際に「答えを見る前に解けている」可能性もある） | Phase 1 の選択肢コミットで客観データが取れれば判別できる。**それまで `STATUS_PROB` は触らない**（推測で定数を動かすのは、まさに今回やめたい行為） |

---

## 7. 結論 ―― 次にやるべきことの順序

実データを踏まえると、優先順位は以下になる。

1. **Phase 0（S の17問バックフィル）** ―― 単独で即マージ。合格の確実性に直接効く既知の欠損
2. **残り5章（208問）に着手する** ―― コードではなく運用。これが最大かつ唯一の律速。
   CBT模試による検証は、範囲がある程度埋まってからでないと「実力不足」と「未学習範囲」を
   切り分けられないため、いま受けても意味が薄い（2026-09-03 の判断）
3. **Phase A（可視化）** ―― 「現状が分からない」への直接の回答。自動で動かす前に見えるようにする
4. **Phase B（今日のラインを容量ベースへ）** ―― 復帰時の「36問・4時間」の崖を消す。体感が最も変わる
5. **Phase 1（解答前コミット / 別設計書）と Phase C（保持率の自動最適化）** ―― 並行可
6. **Phase D（`w[]` 最適化）** ―― 演習1,000件超・Rating 4値が流れてから

**要求3への回答（訂正版）**: 合格ライン＋α を守るのに必要なのは **1日約50分**であり、
現状は達成に遠い。「勉強できない日が続く」ことへの正しい対処は、目標を下げることでも
ノルマを下げることでもなく、**総量を保ったまま、その日の限られた時間を最も点数に効く順に
使うこと**である。積み残しは消さず、翌日以降に必ず戻す。

したがって Phase B の「コア集合」は、**合格に必要な最小集合から作ってはならない**
（それは「やる量を減らしてよい」と言う仕組みであり、原則 §0 に反する）。
コアは「残り5章208問 ÷ 残り日数」を基準とし、今日の絞り込みは**順序付けだけ**に使う。

---

## 8. 参照

- `docs/design/calculation-active-learning.md` — 解答前コミット（Phase 1）。本書の `attempt` と `policy` は同じ JSONB の器を共有する
- `docs/design/study-time-scarcity.md` — 課題2（目標の乖離）・課題5（保持率）・課題11・課題13
- `docs/design/expansion-design.md` — §7.2 ペース分析・§7.3 復習最適化
- `CLAUDE.md` — コンフリクト最小化方針
