# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際に必ず参照する方針書です。

## コンフリクト最小化方針（最優先で遵守）

このリポジトリでは、複数の Claude Code セッション（並行実行・別ブランチでの作業）が
同時多発的に PR を作成する運用を前提とする。**マージ前のコンフリクトはレビュー・マージの
ボトルネックになるため、設計・実装のあらゆる判断において「他PRとぶつかりにくい形」を
既定の選択肢とすること。** 「動けば良い」ではなく「他の変更と同時に存在してもマージしやすい」
ことを実装の一部として扱う。

### 1. 変更は狭く・小さく・単機能に閉じる

- 1つの PR で触るファイルは必要最小限に絞る。ついでの整形・リファクタ・import 並び替えなど
  無関係な変更を feature の diff に混ぜない（"ドライブバイ" 修正は禁止）。
- 大きな機能追加は、可能な限り「新規ファイルの追加」で実現し、既存の大きな共有ファイルへの
  変更を避ける。既存ファイルを触る必要がある場合も、変更行数を最小にする。
- 1PRのスコープが大きくなりそうな場合は、Phase分割・ファイル分割を先に行い、独立してマージ
  できる単位に切り出す（`docs/design/expansion-design.md` の Phase 分割はこの考え方の実例）。

### 2. 共有ファイル・共有シンボルへの追記は「末尾追加」を基本にする

- 配列・オブジェクト・レジストリ（例: `EXAMS`, `PAPERS`, `QUESTIONS` 系、ルーティング定義など）
  へ要素を追加する際は、**既存要素の並び替え・削除を伴わず、末尾に追加**する。同じ場所に複数の
  PR が同時に要素を足すと必ずコンフリクトするため、追加位置を分散させない。
- 既存のオブジェクト/配列リテラルの末尾には常に **trailing comma** を付ける。次の要素追加が
  1行差分で済むようにする。
- 大きな1ファイル（例: `src/App.tsx` のような肥大化したファイル）に新しいロジックを追記する
  运用は避け、`docs/design/expansion-design.md` の方針どおり `features/` や `data/` 配下の
  独立ファイルに分割してから追加する。1ファイルが大きいほど、同時編集によるコンフリクト確率が
  上がることを常に意識する。

### 3. データ追加はファイル単位・ID単位で分離する

- 章・年度・資格などの単位でデータを追加する場合は、既存データファイルを直接編集せず、
  新しいファイル（例: `src/data/denken3/riron/papers/r7-1.ts`）を追加し、末尾の index/registry
  にのみ1行追記する形にする。
- ID・キーの命名は衝突しない接頭辞規則（既存: `exam_id`/`question_id` のスコープ設計）に従う。
  新規追加のたびに既存IDの採番ルールを変更しない。

### 4. DBマイグレーションは採番の早取り・破壊的変更回避を徹底する

- マイグレーション番号は「作業着手時」ではなく「実装完了・コミット直前」に確認し直し、
  他PRで既に使われていないか確認してから採番する（連番の奪い合いがコンフリクトの典型例）。
- 既存カラム・既存制約の変更・削除は避け、追加（`ADD COLUMN`、新規テーブル）で表現する
  （`docs/design/expansion-design.md` の migration 008 の方針を標準とする）。

### 5. 作業開始前・PR作成前の同期

- 実装に着手する前に、必ず最新の base ブランチを fetch し、同時期に走っている他の変更内容
  （オープン中のPR・直近マージ済みコミット）を確認する。同じファイル・同じ追記位置を狙って
  いないかを事前に把握する。
- 実装完了後、push・PR作成前にも base ブランチを取り込み、コンフリクトが無いことをローカルで
  確認してから PR を作成する。

### 6. フォーマッタ・Lintの一括整形は変更に混ぜない

- 保存時整形やエディタの自動整形で、編集していない行のスタイルが変わっていないか diff を
  確認する。無関係な空白・改行・並び替えの変更はコンフリクトの主要因になるため、機能追加の
  PR には含めない。

---

上記は「コンフリクトを完全にゼロにする」ためのルールではなく、**設計段階から
「他の変更と同時に存在してもマージしやすい形」を選ぶ**ための既定方針である。
新しい設計書（`docs/design/*.md`）を書く際も、この方針に反する構成（単一巨大ファイルへの
継続的な追記、共有シンボルの並び替えを伴う変更など）を採用しないこと。
